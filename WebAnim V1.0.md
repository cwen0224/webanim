# **概念**

我想要設計一個十分簡單的網頁動畫軟體，主要以控制png、svg為核心，可以梯形變形、旋轉、縮放、改變透明度，來產生簡易的動畫效果。另外，這個軟體有十分詳細的命名原則，可以讓AI很好的知道畫面中的東西是甚麼。主要的變形機制很簡單，所以主要是要做成 3/4 視角 (Three Quarter View) 或是測視角(Lateral View)的限制動畫 (Limited Animation)，或者該說剪紙動畫 (Cutout Animation) 為核心目標。聚焦在人物的表情以及手部的動作，以文戲居多，不太會有動作戲的場面。主要分成建模、參數運算以及動畫、剪接，四個功能。

# **建模**

首先要做的是先製作2D角色建模，之後才可以用這個建模去做動作。

1. 2D建模：參考Live2D、Inochi2D還有Spine、DragonBones設計一個可以在網頁裡面運行的2D建模動畫軟體，但是先不導入多邊形變形功能，而是先採用梯形變形。  
2. 梯形變形：最基礎的功能，是可以貼入透明的png，然後可以梯形變形、旋轉、縮放、改變透明度，來產生簡易的動畫效果。  
3. 素材切換：可以通過設定的觸發機制，比方說類似Live2D的參數滑桿，或是類似Spine的IK，來切換素材png圖片，可以用在眨眼，或是補間禎。可以透過綁定到參數來設置。  
4. 父層影響：可以把多張png套用在同一個變形器內，然後，相同變形器內的PNG會隨變形器梯形變形、旋轉、縮放、改變透明度。而變形器也可以放到其他變形器內，所以可以做到層層堆疊。  
5. 重心機制：重心機制主要是用來製作出關節。  
   1. 重心定義：所有物件，也就是所有png和變形器，都可以定義重心，這個重心是此物件旋轉時的軸心，如果沒有定義，會先放在物件正中央。  
   2. 重心插銷：物件只會有一個重心，但是可以有多個插銷，插銷就是用來跟其他物件的重心做綁定用的，一個插銷可以跟多個重心綁定，但是一個重心不能與多個插銷綁定。  
   3. 綁定父子：如果重心與插銷綁定，重心件會成為插銷件的從動件。子極會隨父級移動變化位置與大小與轉動。  
   4. 重心最高：所有變形方式，都會以不改變兩張素材的重心與插銷連接位置為先決條件，它們或許會一起移動，但是直到解開綁定為止，都不會分開。  
   5. 重心插銷心內部座標：定義一個重心或插銷時，系統不應該記錄它在整個畫布上的座標，而是記錄它在該物件四邊形內的相對座標位置，而且要用百分比（u,v）座標，這樣可以確保這個位置永遠不會改變。  
   6. 重心名字：每個重心插銷都可以有名字去定義，這樣比較方便AI了解這個模型是如何運作的，比方說我可以把一個重心插銷命名為「左手手肘面向右側」這樣一來，AI就知道，這是當畫面中的左手窩面向右側時，左手前臂與上臂連接的位置。  
   7. 綁定狀態：綁定狀態下，就好比兩張紙在這一個像素位置上，被用圖釘釘在一起，這兩個像素，無論在甚麼情況，都不會發生位移。  
6. 參數滑桿：主要的動作可以靠創造新參數來綁定，類似Live2D的機制。  
   1. 參數綁定：當我把一個物件（變形器與png素材的統稱）綁定到一個參數，那就會在參數的頭尾出現關鍵禎標誌。關鍵禎也可以增加。  
   2. 四角位置運算：後臺會隨時記錄所有在畫面中的物件的四個角的位置，就像是多邊形的3D動畫會記錄頂點，但是我這裡比較輕鬆，只要記錄四個頂點，而至於重心、插銷則是用算法推算出來。  
   3. 動作狀態紀錄：後臺會記錄下這時png還有變形器的四個角落的位置，然後，當我改變參數，到有關鍵禎標誌的參數時，就可以設定這個物件的下一個動作，然後，我只要滑動參數滑桿，就可以看到這個物件在兩個動作間變形。  
   4. 參數命名：參數會有詳細的命名，讓AI可以很好了解這個動作參數是跟甚麼相關，比方說「眨左眼」。  
   5. 轉動優先：這個運動過程會以轉動優先，如果可以透過轉動來變形的部分，就會先轉動，然後以定義過的重心為軸心旋轉。  
7. 十字參數混和：當同一個物件有X、Y兩個參數滑桿需要參照，就可以將兩個參數混合，變成十字參數。  
   1. 變成方塊：滑桿會變成一個可以在一個平面方塊上滑動的狀態，一樣可以在每個點上增加關鍵幀標記。  
   2. 角落加算：當兩個動作分開做好後，所有動作都可以在各個角落合計，但是仍然可以二次編輯，再度調整，讓整個變形算法，變成在四個角之間運算。  
8. 參數雷達：如果是線性的動畫，就會採用滑桿，但是如果是要切換素材來產生動態感，就會是參數雷達。這是為了讓所有表情可以順利在不同表情間切換，但是又不會中途出現不該出現的表情。  
   1. 圓形參數：由0到360的角度值與一個代表離心長度的0到100半徑值決定，看起來像是一個雷達一樣。  
   2. 基礎狀態：通常會用點處於哪個扇形與半徑長度為判斷基準，基礎狀態會是處於圓心，會有角度參數，但是看不到，因為長度為0。這時會是最基礎的狀態。比方說，如果是人的眼睛，我就會設定長度5以下，全角度，就會處於無表情的張開狀態，然後五以上0\~30度會是笑的眼睛。  
   3. 扇形分布：我可以把型態比較接近的素材，放在比較靠近的扇區，這樣就算中間經過了那些素材，也不至於太突兀，或是每次都固定先從中間座標（0度，0半徑）穿過去。  
9. 遮罩：這個系統有相當複雜的遮罩與反遮罩功能，還可以用多重遮罩搭配使用。  
   1. 正遮罩：可以把黑白的png設置為正遮罩，這時，只有與白色位置相重疊的位置會被展現。平常狀態下不會顯示出來，要開啟「顯示正遮罩」功能，才可以看到他們的本體。  
   2. 反遮罩：可以把黑白的png設置為反遮罩，這時，只有與黑色位置相重疊的位置不會展現。平常狀態下不會顯示出來，要開啟「顯示反遮罩」功能，才可以看到他們的本體。  
   3. 描邊遮罩：任意圖片都可以設置為描邊遮罩，主要是用在普通素材png上，它能自動偵測此物件的透明邊界，讓內容僅顯示在「輪廓線」之上，也可以先將多個物件用變形器整合，然後讓這整組png素材的邊框都被參考到。  
   4. 雙重遮罩：可以將多組遮罩設置為and遮罩，要在兩個遮罩的重疊區域才會顯示出來。   
10. 參數運算：參數間可以用公式來產生連動效果，這個公式可以自己設定，只要把規則輸入給AI，也可以用AI來生成。用這個方法，就可以製作出IK功能。  
11. 物件管理：所有物件除了顯示在圖層整理器，還會顯示在變形器整理器、資料夾整理器裡面，是用不同的規則在整理。  
    1. 圖層整理器：它會把所有物件按照圖層的高低排列，也可以創資料夾來包裹，這裡只會出現png，不會出現變形器。  
    2. 變形器整理器：它會把物件放在所屬的上層變形器內，類似Photoshop的群組化功能。  
    3. 資料夾整理器：它會把物件先按照圖層排列，但是可以自由安排到不同的資料夾，這裡的變動不會影響到圖層高低，這裡也可以用物件名稱搜尋系統來篩選物件。

# **參數運算器**

透過參數計算器的輸出與回授，可以讓動畫製作過程變得十分快速簡單。

1. 驅動參數：可以先把幾個空白參數設置位輸入參數，讓大部分在動畫階段要進行的操作，都在這類型的參數內完成，其實也就是沒有直接綁定動作的空參數，並不需要特別定義過。  
2. 連動參數：隨著驅動參數的變化，可以設定一個運算過程，讓其他任何參數產生的變化，進一步產生連動參數的變化，而連動參數的變化，還可以近一步推動其它連動參數的變化，如此一來，就可以產生IK還有頭髮物理效應的算法。要注意的是，得避免使用者產生有放大效應的回授迴圈，因為這樣會導致所有連動動作被無限放大循環。

# **動畫**

有了角色建模之後，就可以把多個角色放到布景內設置動作，主要是採用節點編輯器的格式，但是也可以切換為類似常規動畫軟體的時間軸與關鍵幀模式來編輯。

1. 參數控制：參數控制主要用節點編輯器去進行設計，可以直接右鍵複製到剪貼簿，所以可以貼去給AI，然後讓AI調整後，再回傳，或是一開始就讓AI產生，這個交互就是用獨特的JSON格式去構成。  
2. 參數狀態節點：使用者要設定一個「參數狀態節點」他們想要呼喚的驅動或連動參數，然後設定某個時間，跟這個時間點的那些參數的值。  
3. 等待節點：參數節點後面要連接上一個「等待節點」，這裡可以填寫時間，然後再接上下一個「參數狀態節點」，然後系統就會嘗試讓畫面呈現出在這兩個節點間的動態，後面那個餐屬狀態節點的時間，會直接變成上一個參數節點加上等待節點內填寫的時間參數。  
4. 多節點線合計：而一個角色，可能會在同一時段，會有兩條這樣的節點線需要參考，那這時，系統就會把這兩著在運算完參數運算器的值後得出來的值，加算成一個最終的值。  
5. 分開設計動作：通常應該是會把不同動作參數用不同節點呼喚，然後最後再加算，這樣動畫師比較好掌握每個動作的節奏。  
6. 嘴音同步：可以導入音效，然後根據錄音，來產生AEIOU和閉嘴的五種嘴型。可以採用Rhubarb Lip-Sync。  
7. 角色重心插銷：角色與角色之間，也可以用插銷來相互綁定，比方說一個特別壯的角色，可以在他的手掌上放一個插銷，然後在另一個瘦小的角色的手掌增加一個角色重心，角色重心會無視先前的父子層級，反而以此點作為最高層級的重心，如此一來，把這個瘦小的角色的角色重心綁定到壯角色手上的插銷，就可以做出浩克甩洛基的動畫。

# **剪接**

動畫都做好後，可以把每個動畫變成一個個片段，包含他們用來對口型的錄音，直接用內建的剪接軟體剪接出來。

1. 片段節點：每個動畫片段可以做為一個動畫節點，這裡需要填寫進起始時間，還有結束時間。  
2. 等待節點：這功能就跟動畫內部的那個是一樣的東西，會在兩個片段間差一個空檔。  
3. 多線呈現：可以弄多條連接好的片段，然後她會根據時間自動安排，在節點編輯器中，高度Y座標越高的，圖層越高，可以用這個功能來。