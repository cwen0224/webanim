# WebAnim — 網頁 2D 剪紙動畫軟體

## 專案概要
WebAnim 是一個在瀏覽器中運行的 2D 限制動畫（剪紙動畫）製作工具，以 3/4 視角與側視角人物動畫為核心目標。概念參考 Live2D、Spine、DragonBones，但設計更輕量，聚焦於 PNG/SVG 素材的梯形變形動畫，不使用多邊形網格變形。

## 技術棧
| 用途 | 選擇 | 理由 |
|------|------|------|
| 渲染 | PixiJS v8 + WebGL | 原生支援 sprite、mask、filter，梯形變換直接用 homography matrix |
| 語言 | TypeScript | 複雜資料結構需要型別安全 |
| 建構 | Vite | 快速 HMR，開發體驗好 |
| UI 框架 | React | 工具列、面板、參數列表等 UI 元件 |
| 節點編輯器 | 自製或 litegraph.js | 節點格式需要配合 JSON AI 交互設計 |
| 數學 | gl-matrix | 矩陣運算、向量計算 |
| 公式解析 | math.js | 參數運算器的公式字串求值 |
| 嘴型同步 | Rhubarb Lip-Sync | 後期整合，優先用 WASM 版本 |

## 四大模組
1. **建模（Modeler）** — 梯形變形、重心插銷系統、參數滑桿、十字參數、參數雷達、遮罩、物件管理
2. **參數運算器（Calculator）** — 驅動參數、連動參數、公式引擎、IK、防迴圈 DAG 驗證
3. **動畫（Animator）** — 節點編輯器、時間軸關鍵幀、多線加算、嘴型同步
4. **剪接（Editor）** — 片段節點、等待節點、多線時間軸

## 核心設計原則
- **四角頂點是唯一真實狀態**：所有物件的狀態以四個角落的世界座標記錄，重心、插銷位置由此推算。
- **UV 相對座標**：重心與插銷的位置以物件四邊形內的 (u, v) 百分比儲存，確保縮放後不偏移。
- **DAG 連動圖**：參數連動必須是有向無環圖，系統在建立連動前驗證不形成循環。
- **AI 可讀 JSON**：節點編輯器資料、參數命名、重心插銷命名，均以易讀 JSON 設計，方便 AI 生成與修改。
- **轉動優先**：兩狀態間的插值，優先以重心為軸做旋轉，旋轉無法解釋的部分再做縮放/位移。

## 詳細文件
- [技術架構與開發路線圖](docs/architecture.md)
- [原始規格文件](WebAnim%20V1.0.md)
